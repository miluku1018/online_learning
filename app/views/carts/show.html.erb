<h1 class="title">購物車</h1>

<%= link_to "清空購物車", cart_path, method: 'delete',class: 'btn btn-warning float-right' if not current_cart.empty? %>

<%= render 'cart', cart: current_cart %>



<%= link_to "繼續購物", root_path, class: 'btn btn-primary mt-2' %>

